# Django site requirements
# Install from the repo root with: pip install -r django_site/requirements.txt

# Shared preprint-source package (taxonomy for the category picker).
./packages/preprint_sources

Django>=5.1,<6.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
pgvector>=0.3.6

# Only needed if you want the paper-add-from-arXiv feature to work
requests>=2.31.0
arxiv>=2.1.0

# PDF text layer validation on upload
pypdf>=4.0.0

# Decode LaTeX author names to Unicode (used by the 0013 backfill migration)
pylatexenc>=2.10

# WSGI server for production
gunicorn>=21.2
