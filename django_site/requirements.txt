# Django site requirements
# Install from the repo root with: pip install -r django_site/requirements.txt

# Shared preprint-source package: taxonomy for the category picker, plus the
# fetch/search/landing-URL behaviour behind the add-paper tabs.
./packages/preprint_sources

Django>=5.1,<6.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
pgvector>=0.3.6

# PDF downloads for the add-by-ID flow
requests>=2.31.0
# Only needed by the refresh_arxiv_metadata management command
arxiv>=2.1.0

# PDF text layer validation on upload
pypdf>=4.0.0

# Decode LaTeX author names to Unicode (used by the 0013 backfill migration)
pylatexenc>=2.10

# WSGI server for production
gunicorn>=21.2
